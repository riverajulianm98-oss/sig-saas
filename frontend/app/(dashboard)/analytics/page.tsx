import { AnalyticsView } from '@/modules/reports/views/analytics-view'

export const metadata = { title: 'Centro Ejecutivo — SIG SaaS' }

export default function AnalyticsPage() {
  return <AnalyticsView />
}
