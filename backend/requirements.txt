# SIG SaaS — Backend dependencies
# Pin versions in production; regenerate with: pip freeze > requirements.lock

fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.20

# Database
SQLAlchemy==2.0.49
alembic==1.18.4
psycopg2-binary==2.9.12

# Configuration & validation
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# Security (prepared for auth module — not wired yet)
python-jose[cryptography]==3.5.0
bcrypt==4.2.1
email-validator==2.2.0

# Object storage (S3 / MinIO / R2)
boto3==1.37.38

# Dev / test
httpx==0.28.1
pytest==8.3.5
pytest-asyncio==0.26.0
